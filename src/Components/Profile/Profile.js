import React from 'react';
import styles from './Profile.css'

const Profile = ({position}) => {
    return(
        <div>
            <p>
                {`${position}`}
            </p>

        </div>
    )
}

export default Profile